---
description: gaudio 核心工作流 — 原则、执行、验证与完成标准（来自 AGENTS.md）
alwaysApply: true
---

# gaudio 核心工作流

## 原则

- 先理解根因再改代码；修原因，不修表象。
- 遵循现有架构与约定，除非与正确性冲突。
- 选能完整解决问题的最简方案。

## 执行

- 低风险、可回滚的改动默认直接做；少问不必要的确认。
- 只在真正需要人工决策时停下；不擅自扩大范围。
- 改前先理解现有实现；优先改现有文件；保持 diff 小且聚焦。
- 仅在与当前任务直接相关时才重构相邻代码。

## 验证

- 以实际执行为准；适用时跑 Type Check、Lint、Tests、Build。
- 未执行的验证不得声称已通过。
- 结论标注：Executed / Inspected / Assumed。

## 需确认的高风险改动

- 数据库 schema、生产数据、公开 API、鉴权授权、大型跨模块重构、不可逆操作。

## 禁止

- 大面积无谓重写、无理由 breaking change、禁用 lint、忽略 TS 错误、用 `any` 偷懒、改无关文件、无意义 AI 注释、仅为字段映射写 helper、使用 `normalize*` 作为方法/变量/文件/类型名。

## 完成标准

- 实现完成；相关验证完成；风险与假设已说明；范围可控；代码可维护。
